1) ติดตั้งในเครื่อง (ติดตั้งครั้งเดียว)

Node.js (LTS) → มาพร้อม npm

เช็คได้ด้วย:

node -v
npm -v

ถ้าไม่มี ให้ติดตั้งจาก nodejs.org (เลือก LTS)

2) ติดตั้งในโปรเจกต์ (ทำครั้งแรกต่อโปรเจกต์)

เข้าโฟลเดอร์โปรเจกต์ yeumeasy-frontend แล้วรัน:

npm install
3) แพ็กเกจที่โปรเจกต์นี้ควรมี (ที่คุณใช้จริง)

จากที่เราทำกัน จะมีหลัก ๆ:

react

react-dom

vite (มากับ Vite project)

react-router-dom (ทำหน้า/เมนู routing)

axios (ถ้าคุณใช้ services/api.js)

ถ้าจะติดตั้งแยกทีละตัว (กรณีขาดบางตัว):

npm install react-router-dom axios
4) วิธีรัน
npm run dev
5) เช็คว่าเครื่องคุณ “ครบจริง” ด้วยคำสั่งนี้
npm ls react react-dom react-router-dom axios vite
หมายเหตุ

ไฟล์ที่คุณเคยอัปโหลดให้ผมก่อนหน้านี้ในแชทนี้บางไฟล์ “หมดอายุแล้ว” แต่ ไม่กระทบการรันในเครื่องคุณ นะครับ เพราะไฟล์จริงอยู่ในโปรเจกต์คุณอยู่แล้ว

ถ้าคุณส่ง “หน้า package.json ส่วน dependencies + scripts” มา ผมเช็คให้ได้เลยว่าขาดตัวไหนบ้างและควรติดตั้งอะไรเพิ่มครับ

รันไฟล
npm run dev